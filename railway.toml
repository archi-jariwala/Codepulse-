[build]
builder = "dockerfile"
dockerfilePath = "codepulse-ingest/Dockerfile"

[deploy]
startCommand = "node codepulse-ingest/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
